import { RoutePlaceholder } from "@/components/route-placeholder";

export default function LaunchPlanPage() {
  return (
    <RoutePlaceholder
      title="Launch Plan"
      summary="The generated Markdown launch plan is available in the main workflow."
    />
  );
}
