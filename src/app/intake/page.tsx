import { RoutePlaceholder } from "@/components/route-placeholder";

export default function IntakePage() {
  return (
    <RoutePlaceholder
      title="Intake"
      summary="Repo URL validation, manual start, and deployment-shaping questions live in the main workflow."
    />
  );
}
