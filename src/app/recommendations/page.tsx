import { RoutePlaceholder } from "@/components/route-placeholder";

export default function RecommendationsPage() {
  return (
    <RoutePlaceholder
      title="Recommendations"
      summary="Comparable stack options are generated in the main workflow."
    />
  );
}
