import { RoutePlaceholder } from "@/components/route-placeholder";

export default function ExportPage() {
  return (
    <RoutePlaceholder
      title="Export"
      summary="Copy actions and Markdown export placeholders are available in the main workflow."
    />
  );
}
