import { RoutePlaceholder } from "@/components/route-placeholder";

export default function AnalysisPage() {
  return (
    <RoutePlaceholder
      title="Analysis"
      summary="Framework, package manager, build command, env vars, services, and risks are shown in the main workflow."
    />
  );
}
